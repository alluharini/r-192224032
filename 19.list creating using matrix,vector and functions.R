# Create a list with vectors, matrices, and a function
my_list <- list(
  vector_element = c(1, 2, 3),
  matrix_element = matrix(4:9, ncol = 2),
  function_element = function(x) x^2
)

# Print the content of the list
print(my_list)
